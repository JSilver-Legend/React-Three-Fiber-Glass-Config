export default class FireResistentGlass {
  constructor(id, glasstype, id_app, id_veras, klasse, thickness, clear_glass, uv_protection, application, structure, manufacturer, u_value, sound, weight, butt_joints, miter, resistance, max_width, max_height, order) {
  	this.id = id;
  	this.glasstype = glasstype;
  	this.id_app = id_app;
  	this.id_veras = id_veras;
  	this.klasse = klasse;
    this.thickness = thickness;
  	this.clear_glass = clear_glass;
    this.uv_protection = uv_protection;
  	this.application = application;
    this.structure = structure;
  	this.manufacturer = manufacturer;
		this.u_value = u_value;
    this.sound = sound;
		this.weight = weight;
		this.butt_joints = butt_joints;
    this.miter = miter;
		this.resistance = resistance;
		this.max_width = max_width;
		this.max_height = max_height;
    this.order = order;
	}
}

